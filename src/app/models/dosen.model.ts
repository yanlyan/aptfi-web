export interface Dosen {
  id: bigint;
  uuid: string;
  dosenName: string;
  dosenTitle: string;
  dosenNidn: string;
  dosenBirthDate: Date;
  dosenPosition: string;
  dosenExpertise: string;
  dosenProdi: string;
  dosenType: string;
}
